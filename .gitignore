# CMake build output
build/
# 2026-09 RP2350 (Pico 2 W) 專用的另一個 build 目錄，跟上面的 build/ 分開
# 是為了不影響原本 RP2040 (pico_w) 的 build/，同樣不進版控。
build-pico2/
# 2026-09-18 這台電腦另外用過的 build 目錄名稱（跟上面 build-pico2/ 是同一種
# 用途，只是當下沒注意到已經有約定的名字），一併排除。
build2/
build_pico2w/

# GM700SB 實機測試留下的序列埠 log（可能好幾十 MB），純除錯用，不需要進版控。
tools/*.log

# 編輯器/工具產生的暫存檔
.vscode/
.claude/
*.o
*.obj
*.elf
*.uf2
*.bin
*.hex
*.map

# 本機測試伺服器執行時的 log
test_server/server.log
